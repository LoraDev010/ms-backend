spring:
  datasource:
    url: jdbc:postgresql://catalog-db:5432/catalog
    username: catalog
    password: catalog
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
