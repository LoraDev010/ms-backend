spring:
  datasource:
    url: jdbc:postgresql://orders-db:5432/orders
    username: orders
    password: orders
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
