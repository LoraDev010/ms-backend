server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/orders
    username: orders
    password: orders
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
